<h1> <%=@recipient %> </h1>

<% @conversation.receipts_for(current_user).each do |r| %>
	<div>
		<p><%= r.message.sender.email %> sent:</p>
		<p><%= r.message.body %></p>
	</div>
<% end %>

<%= form_tag conversation_messages_path(@conversation), method: :post do %>
	<div>
		<%= text_area_tag :body %>
	</div>

	<%= submit_tag %>
<% end %>